{
  "name": "firstlayer-streamdeck",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --outfile=com.firstlayer.overlay.sdPlugin/bin/plugin.js",
    "watch": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --outfile=com.firstlayer.overlay.sdPlugin/bin/plugin.js --watch"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
